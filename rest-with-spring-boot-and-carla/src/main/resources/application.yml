cors:
  originPatterns: http://localhost:8080,https://spring.io,http://localhost:3000,https://www.erudio.com.br


spring:
  application:
    name: rest-with-spring-boot-and-carla
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/rest_with_spring_boot_and_carla?useTimezone=true&serverTimezone=UTC
    username: root
    password: 1122
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
    show-sql: false

    open-in-view:
spring-doc:
  paths-to-match: /api/**/v1/**
  swagger-ui:
    use-root-path: true